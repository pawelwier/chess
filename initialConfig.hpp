#ifndef INIT_CONFIG
#define INIT_CONFIG

#include <cmath>

extern const unsigned short int SIZE = 8;
extern const unsigned short int FIELD_COUNT = pow(SIZE, 2);
extern const unsigned short int START_LETTER = 65; // A
extern const unsigned short int START_NUMBER = 1;

#endif